<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    android:layout_marginEnd="12dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        android:id="@+id/room_img_container"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:cardCornerRadius="12dp">

        <ImageView
            android:id="@+id/room_img_iv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"/>

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/room_name_tv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:text="Room "
        android:maxLines="1"
        android:layout_marginHorizontal="2dp"
        android:ellipsize="end"
        android:gravity="center"
        app:layout_constraintLeft_toLeftOf="@id/room_img_container"
        app:layout_constraintRight_toRightOf="@id/room_img_container"
        app:layout_constraintTop_toBottomOf="@id/room_img_container"
        android:layout_marginTop="4dp"
        android:textColor="@color/white"
        android:fontFamily="@font/noto_sans"/>

</androidx.constraintlayout.widget.ConstraintLayout>